spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employees
    username: root
    password: mysqlmysql
  jpa:
    open-in-view: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
logging:
  level:
    org.hibernate.orm.jdbc.bind: trace